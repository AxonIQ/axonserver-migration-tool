spring:
  application:
    name: axondb-migration
  profiles:
    active: migrate-from-jpa
  jpa:
    database: default

axoniq:
  migration:
    batchSize: 2

  datasource:
    migration:
      url: jdbc:h2:./migrationdb

axon:
  serializer:
    events: xstream
