spring:
  application:
    name: axonserver-migration
  jpa:
    database: default
  main:
    banner-mode: off
axoniq:
  datasource:
    migration:
      url: jdbc:h2:./migrationdb
  migration:
    batch-size: 100
axon:
  serializer:
    events: jackson
