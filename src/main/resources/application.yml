spring:
  application:
    name: axondb-migration
  profiles:
    active: migrate-from-jpa
  jpa:
    database: default


axoniq:
  datasource:
    eventstore:
      url: jdbc:postgresql://localhost:5432/postgres
      username: postgres
      password: mysecretpassword
    migration:
      url: jdbc:h2:./migrationdb
  migration:
    batch-size: 1000
    method: local
    migrate-events: false
    local:
      event-store-path: /home/morlack/es-data2/
      index-type: JUMP_SKIP
axon:
  serializer:
    events: jackson
  axonserver:
    event:
      storage: /home/morlack/as-data
logging:
  pattern:
    file:
